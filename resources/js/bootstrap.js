/**
 * resources/js/bootstrap.js
 *
 * Kept as a re-export shim so the default `import './bootstrap';` pattern in
 * Laravel scaffolding still resolves. Actual Axios configuration lives in
 * resources/js/app.js (the loaded entry point from vite.config.js).
 */
import './app';
